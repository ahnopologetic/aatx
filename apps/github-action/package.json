{
  "name": "aatx/tracking-plan-validator",
  "version": "1.0.0",
  "description": "GitHub Action to validate analytics tracking code against a central tracking plan",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions",
    "analytics",
    "tracking",
    "validation"
  ],
  "author": "AATX",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}